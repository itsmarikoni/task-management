# 要件定義書 - Trello風タスク管理アプリ

## 1. 目的・背景

- 個人の学習・ポートフォリオとして、Trello風のタスク管理アプリを開発する。
- プログラミング学習の成果物として、実際に動作するWebアプリを完成させることを目指す。

## 2. 利用者(ユーザー)

- 開発者本人が個人で使用する。
- ログイン機能などマルチユーザーは想定しない。

## 3. 機能要件

### 3.1 基本機能

| 機能 | 内容 |
|---|---|
| ボード | プロジェクト単位の作業スペース |
| カラム(リスト) | 「未着手」「進行中」「完了」など、カードを分類する列 |
| カード | 個々のタスクを表す単位 |
| ドラッグ&ドロップ | カードをカラム間でマウス操作により移動できる |

### 3.2 カード(タスク)が持つ情報

- タイトル
- 説明文
- 優先度(例:高・中・低)
- 期限(日付)

### 3.3 CRUD操作

- カードの作成・編集・削除
- カラムの作成・編集・削除

## 4. 画面・操作イメージ

- ボード画面に複数のカラムが横並びで表示される。
- 各カラムの中にカードが縦に並ぶ。
- カードをドラッグして別のカラムにドロップすると、ステータス(カラム)が変わる。
- カードをクリックすると詳細(タイトル・説明文・優先度・期限)を編集できる。

## 5. 非機能要件

### 5.1 データ保存

- ブラウザを閉じてもデータが消えないようにする。
- ブラウザの`localStorage`にデータを保存する(サーバー・データベースは使用しない)。
- 同一端末・同一ブラウザでの利用を前提とする。

### 5.2 動作環境

- モダンブラウザ(Google Chrome等)で動作すること。

## 6. 技術スタック

| 分類 | 使用技術 |
|---|---|
| フロントエンド | HTML / CSS / JavaScript |
| データ保存 | localStorage |
| サーバー・DB | 使用しない |

## 7. 制約・スケジュール

- 特になし(学習ペースで進める)。
